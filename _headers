# Allow any origin to read the games manifest — each game's "Game →" nav fetches
# it cross-origin, including when a game is embedded off-site (e.g. itch.io).
/games.json
  Access-Control-Allow-Origin: *
